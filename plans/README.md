# Implementation Plans

Generated by the improve skill on 2026-07-02 against commit `ca8b8b1f` (branch `dev`).
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, run the drift check
first, and update your row when done.

Repo facts every executor needs: two Next.js apps — the repo root (main product,
"Wakkelni Stars") and `saas-admin/` (platform admin, own package.json). Verification
commands at planning time: `npm run lint`, `npm run typecheck` (= `next build` until
plan 010 changes it), `npm test` (exists only after plan 005). Schema changes go
through `src/lib/db/schema.ts` → `npm run db:generate` → `npm run db:push`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fail-closed auth & cron secrets | P1 | S | — | DONE |
| 002  | Patch vulnerable dependencies (both apps) | P1 | S | — | DONE |
| 003  | Reply-pipeline retry fixes | P1 | M | — | DONE |
| 004  | Durable review dismissal | P1 | M | 003 (soft) | DONE |
| 005  | Vitest baseline + money-path unit tests | P1 | M | — | DONE |
| 006  | Stripe webhook billing correctness | P2 | M | 005 (hard) | DONE |
| 007  | Durable rate limiting on public endpoints | P2 | M | 005 (soft) | DONE |
| 008  | Reviews endpoint SQL pagination + bounded analytics | P2 | L | 005 (hard), 004 (soft) | DONE |
| 009  | Remove dead Geidea code + repo hygiene | P2 | S | — | DONE |
| 010  | Docs refresh, fast typecheck, minimal CI | P3 | S | 005 (soft, for CI test step) | DONE |
| 011  | Design spike: shared DB schema for both apps | P3 | M | 002 (soft) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## UX revamp plans (added 2026-07-12)

Requested by the user: reports-page wizard, square-ui component adoption, and a
new-user product tour. square-ui source lives at
`C:\Users\Lenovo\Desktop\telmeeh\square-ui` (shadcn-style templates; same
Next 16 / React 19 / Tailwind v4 stack).

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 012  | square-ui component foundation + sidebar rebuild | P1 | M | — | TODO |
| 013  | Reports page: "New Report" step-by-step wizard dialog | P1 | M | 012 (hard) | TODO |
| 014  | New-user product tour (tips & spotlight highlights) | P2 | M | 012 (hard), 013 (soft) | TODO |

## Dependency notes

- **006 and 008 require 005**: both refactor money/dashboard paths and must land with tests; 005 creates the harness and the mocking exemplar they copy.
- **004 after 003 (soft)**: both edit the job-pipeline neighborhood; landing 003 first avoids conflicts. Not a functional dependency.
- **008 after 004 (soft)**: plan 004 adds a `dismissedAt` filter to the reviews endpoint that 008's SQL rewrite must carry through — 008's drift check calls this out.
- **010's CI** uses `npm test --if-present`, so it can land before 005; remove the flag after 005.
- **011 after 002 (soft)**: run the spike's feasibility builds against the patched drizzle-orm.
- 001, 002, 009 are independent and safe to execute immediately, in parallel if desired.

## Operator actions surfaced by these plans (not executor work)

- Plan 001: verify `BETTER_AUTH_SECRET`, `ADMIN_BETTER_AUTH_SECRET`, `CRON_SECRET` are set in every deployment env **before** deploying; rotate both auth secrets if any prod deployment ever ran without them (the committed fallback values are burned).
- Plan 009: remove `GEIDEA_*` env vars from deployments; revoke Geidea credentials at the provider.
- Plan 010: make the CI job a required check once green.

## Findings considered and rejected (do not re-audit)

- **Stripe webhook returning 200 on handler errors** — deliberate, commented tradeoff (`webhook/route.ts:307-310`); plan 006 preserves it.
- **`db` possibly undefined across endpoints** — by-design demo-mode degradation (CLAUDE.md); inconsistencies are handled only where they bite (cron fail-open, plan 001).
- **Template fallback when `OPENAI_API_KEY` missing** — by design.
- **Job queue claiming design** (`FOR UPDATE SKIP LOCKED`, backoff, stalled-job requeue) — audited, sound. Minor known edge: a job crashing at exactly max attempts stays `running` until the stall requeue ages out; not worth a plan.
- **Google OAuth token encryption at rest** — defense-in-depth only (standard better-auth storage); M effort; revisit after the P1/P2 backlog clears.
- **Session-lifecycle polish** (`revokeOtherSessions: false` on password change, 60s session cache, workspace cookie `secure` flag) — real but minor; bundle into a future hardening pass.
- **`maxAccounts` check-then-act race** (`google/connect/route.ts:151-166`) — real TOCTOU, needs concurrent requests to matter; revisit if plan limits become contested.
- **Splitting god files** (1,882-line settings page, 1,034-line landing, `admin-queries.ts`) — do only after tests exist; no standalone plan yet.
- **Zod validation standardization across all ~54 routes** — adopt incrementally in touched routes; a big-bang sweep is churn without payoff.
- **Two markdown libraries** (`marked` server-side blog, `react-markdown` chat widget) — both genuinely used; consolidation optional, low value.
- **Landing page as a full client component** — real LCP cost but M–L effort with hydration-regression risk; revisit when marketing traffic justifies it.
- **CSP + HSTS headers** — worth doing (MED risk of breaking Next inline scripts; needs report-only tuning); not selected this round — candidate for the next planning pass.
- **Admin audit-logging gaps** (platform-settings/plan-prices/blog mutations) — small, valuable; not selected this round.
- **Admin blog upload constraints** (MIME allowlist, size cap, key-prefix restriction) — small, valuable; not selected this round.
- **Assistant chat sequential round trips** — partially addressed by plan 008 Step 4 (count caching); full parallelization deferred.
- **Direction options presented but not planned**: review-digest/failed-autopost notification emails (strongest candidate — the user declined a spike for now), actionable AI assistant (requires tenancy tests first), scheduled/emailed comparison reports, Arabic-coverage enforcement for the 753-line static translation map, review-link distribution/tracking.
